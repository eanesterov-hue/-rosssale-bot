# V1 Match: объект → список брокеров по показам

> **Статус: ✅ Реализовано**  
> Полный roadmap проекта: [roadmap.md](roadmap.md)

---

## Зачем делаем

В компании процесс кросс-продаж между брокерами сейчас работает в основном в одну сторону: когда есть клиент → брокер находит объект через WhiteList.

Обратная сторона (появился объект/собственник → найти, у каких брокеров есть активные клиенты под этот объект) делается вручную через чаты и зависит от памяти/инициативы брокера.

V1 решает самый узкий и быстрый кейс: если по этому ЖК/объекту уже идут показы, значит у других брокеров есть "живые" клиенты, и брокеру по объекту нужно быстро увидеть, к кому идти внутри компании.

---

## Что такое V1 (строго)

Локальная утилита, которая по выгрузке "Показы" из AmoCRM возвращает только одно:

**уникальный список имён брокеров**, которые делали показы по заданному объекту/ЖК за выбранный период.

Никаких счётчиков, дат, рейтингов, "теплоты", исключений "уже показывали этого собственника" — это версии V2+.

---

## Быстрый старт

```bash
# Перейти в папку проекта
cd "/Users/jacky/Documents/cross sale"

# Активировать окружение
source venv/bin/activate

# Запустить поиск (файл data/showings.xlsx используется по умолчанию)
python scripts/main.py --object "Прайм парк"
```

---

## Параметры CLI

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--file`, `-f` | Путь к выгрузке `.xlsx` | `data/showings.xlsx` |
| `--object`, `-o` | Строка запроса (название объекта/ЖК) | обязательный |
| `--days`, `-d` | Период в днях | 14 |
| `--match`, `-m` | Режим: `exact` или `contains` | **contains** |
| `--exclude-status`, `-e` | Исключить статус (например: "Отменен") | — |
| `--json`, `-j` | Вывод в формате JSON | — |

---

## Источник данных

Выгрузка "Показы" из AmoCRM (аналитика), таблица со столбцами:

| Колонка | Использование |
|---------|---------------|
| № | не используем |
| **Брокер** | выводим |
| **Дата** | фильтр периода |
| **Объект** | ключ для поиска |
| Статус | опционально для фильтра |

---

## Режимы поиска

- **contains** (по умолчанию) — запрос содержится в названии объекта  
  `"Прайм парк"` найдёт и "Прайм парк", и "Прайм парк вторичка"

- **exact** — строгое совпадение  
  `"Прайм парк"` найдёт только "Прайм парк"

Нормализация: trim + lower + схлопывание двойных пробелов.

---

## Примеры

```bash
# Поиск по объекту (файл data/showings.xlsx по умолчанию)
python scripts/main.py -o "Прайм парк"

# Строгое совпадение
python scripts/main.py -o "Прайм парк" --match exact

# Исключить отменённые показы
python scripts/main.py -o "Lucky" -e "Отменен"

# Вывод в JSON
python scripts/main.py -o "Агаларов" --json

# Указать другой файл
python scripts/main.py -f "другой_файл.xlsx" -o "Lucky"
```

---

## ✅ Критерии готовности (выполнено)

| Запрос | Ожидание | Статус |
|--------|----------|--------|
| `--object "Агаларов"` | Надежда Тюваева | ✅ |
| `--object "Primavera"` | Юлия Пименова (+ другие) | ✅ |
| `--object "Прайм парк"` | Ирина Партионова, Алиса Мяктинова | ✅ |

---

## Ограничения V1 (осознанно)

- Не матчим по бюджету/комнатности/району
- Не читаем комментарии/звонки/теги
- Не учитываем, показывали ли уже конкретный объект (это V2)
- Не делаем UI — только CLI + JSON для будущего интерфейса

---

## Telegram-бот (следующий этап)

### Концепция
Брокер пишет в бот название ЖК → получает список коллег, которые вели показы по этому ЖК.

### Требования

| Параметр | Значение |
|----------|----------|
| Ввод | Текстом (могут быть ошибки в написании) |
| Умный поиск | Бот сам угадывает ближайший объект (fuzzy match) |
| Вывод | Список имён брокеров + период (14 дней) |
| Если не найдено | "Не было показов по этому ЖК за 14 дней" |
| Пользователи | ~100 брокеров, один бот на всех |
| Whitelist | Потом (на тесте не нужен) |
| Интерфейс | Максимально простой (пользователи не технари) |
| Обновление данных | Раз в день (пока вручную, потом автоматом) |

### Пример диалога

```
👤 Брокер: Праймпарк

🤖 Бот:
🏠 Прайм парк (по запросу "Праймпарк")

За последние 14 дней показы вели:
• Ирина Партионова
• Алиса Мяктинова
```

```
👤 Брокер: какой-то несуществующий жк

🤖 Бот:
Не найдено показов по этому ЖК за последние 14 дней.
```

---

## Что дальше

См. [roadmap.md](roadmap.md):
- **V2**: исключать брокеров, которые уже показывали конкретный лот
- **V3**: мэтчинг через комментарии и транскрипты звонков
